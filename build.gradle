buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.7.+'
    }

    defaultTasks = ['clean', ':app:compileDebugNdk', 'test', 'checkstyle', 'pmd', 'lint']
}
